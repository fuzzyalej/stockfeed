{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); cmd=$(echo \"$input\" | jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -qE '^git push'; then uv run mypy src/ 2>&1; fi",
            "timeout": 60,
            "statusMessage": "Running mypy type check..."
          }
        ]
      }
    ]
  }
}
